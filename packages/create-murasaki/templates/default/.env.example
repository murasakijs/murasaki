# Embedded into renderer JavaScript at build time. Never put secrets here.
MURASAKI_PUBLIC_API_ORIGIN=https://api.example.com

# Available only to Murasaki config, plugins, Node Main, Server Actions, and API Routes.
ACCOUNT_API_TOKEN=
