## Summary

<!-- What does this PR do, and why? The diff already shows the "what" — explain the "why" here. -->

Fixes #

## Checklist

- [ ] Tests added/updated for this change (or a manual verification note below if tests don't apply)
- [ ] `pnpm --filter murasaki test` passes locally
- [ ] `cargo test` passes locally (only if you touched `crates/native`)
- [ ] Docs updated in **English and Japanese, 1:1**, if user-facing behavior changed
- [ ] If you changed behavior, [`capabilities.json`](https://github.com/murasakijs/murasaki/blob/main/packages/murasaki/capabilities.json) is updated in the same PR — this repo treats the capability manifest as a contract, and CI validates it

## Manual verification

<!-- If this touches runtime/CLI behavior and isn't fully covered by automated tests, describe how you checked it (OS, command, observed result). -->
