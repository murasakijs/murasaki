name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

env:
  CI: true
  NODE_VERSION: 22.12.0

jobs:
  ubuntu-quality:
    name: Ubuntu / build, test, docs, scaffold
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify supported Node floor
        shell: bash
        run: |
          set -euo pipefail
          test "$(node -p "require('./packages/murasaki/package.json').engines.node")" = ">=22.12.0"
          test "$(node -p "require('./crates/native/package.json').engines.node")" = ">=22.12.0"
          test "$(node -p "require('./packages/create-murasaki/templates/default/package.json').engines.node")" = ">=22.12.0"
          node -e "const [major, minor] = process.versions.node.split('.').map(Number); if (major < 22 || (major === 22 && minor < 12)) process.exit(1)"

      - name: Build workspace packages
        run: pnpm build

      - name: Verify public API reports
        run: pnpm --filter murasaki api:check

      - name: Test Murasaki framework
        run: pnpm --filter murasaki test

      - name: Test MCP server and generated knowledge
        run: |
          pnpm --filter @murasakijs/mcp test
          git diff --exit-code -- packages/mcp/content

      - name: Type-check documentation site
        run: pnpm --filter murasaki-docs types:check

      - name: Test documentation navigation and component catalog
        run: pnpm --filter murasaki-docs test

      - name: Build documentation site
        run: pnpm --filter murasaki-docs build

      - name: Fresh scaffold install, lint, type-check, and build
        shell: bash
        run: |
          set -euo pipefail
          PACK_DIR="$RUNNER_TEMP/murasaki-packs"
          APP_DIR="$RUNNER_TEMP/fresh-murasaki"
          mkdir -p "$PACK_DIR"

          pnpm --dir packages/ui pack --pack-destination "$PACK_DIR"
          pnpm --dir crates/native pack --pack-destination "$PACK_DIR"
          pnpm --dir packages/murasaki pack --pack-destination "$PACK_DIR"
          pnpm --dir packages/create-murasaki pack --pack-destination "$PACK_DIR"

          CREATE_TARBALL=$(find "$PACK_DIR" -maxdepth 1 -name 'create-murasaki-*.tgz' -print -quit)
          test -n "$CREATE_TARBALL"
          CREATE_PACKAGE_DIR="$RUNNER_TEMP/create-murasaki-package"
          mkdir -p "$CREATE_PACKAGE_DIR"
          tar -xzf "$CREATE_TARBALL" -C "$CREATE_PACKAGE_DIR"

          # --yes + --skip-install must be a dependency-free automation path.
          # This prevents package-manager store/link issues in optional prompt
          # UIs from stopping CI, scripts, or unattended Windows scaffolding.
          (
            cd "$RUNNER_TEMP"
            node "$CREATE_PACKAGE_DIR/package/index.mjs" dependency-free-murasaki \
              --yes --linter none --skip-install --no-git
          )
          test -f "$RUNNER_TEMP/dependency-free-murasaki/package.json"

          # Execute the packed CLI as consumers do: with its own production
          # dependencies installed beside the tarball contents. Calling the
          # extracted index.mjs directly without this install only tests an
          # impossible package layout and fails before scaffolding starts.
          pnpm --dir "$CREATE_PACKAGE_DIR/package" install \
            --prod --ignore-workspace --no-frozen-lockfile

          (
            cd "$RUNNER_TEMP"
            node "$CREATE_PACKAGE_DIR/package/index.mjs" fresh-murasaki \
              --yes --linter biome --skip-install --no-git
          )

          UI_TARBALL=$(find "$PACK_DIR" -maxdepth 1 -name 'murasakijs-ui-*.tgz' -print -quit)
          NATIVE_TARBALL=$(find "$PACK_DIR" -maxdepth 1 -name 'murasakijs-native-*.tgz' -print -quit)
          MURASAKI_TARBALL=$(find "$PACK_DIR" -maxdepth 1 -name 'murasaki-[0-9]*.tgz' -print -quit)
          test -n "$UI_TARBALL"
          test -n "$NATIVE_TARBALL"
          test -n "$MURASAKI_TARBALL"

          APP_DIR="$APP_DIR" UI_TARBALL="$UI_TARBALL" NATIVE_TARBALL="$NATIVE_TARBALL" MURASAKI_TARBALL="$MURASAKI_TARBALL" node <<'NODE'
          const fs = require('node:fs')
          const path = require('node:path')
          const manifestPath = path.join(process.env.APP_DIR, 'package.json')
          const manifest = JSON.parse(fs.readFileSync(manifestPath, 'utf8'))
          manifest.dependencies.murasaki = `file:${process.env.MURASAKI_TARBALL}`
          manifest.dependencies['@murasakijs/ui'] = `file:${process.env.UI_TARBALL}`
          // The framework's matching native version is intentionally not on
          // npm until the native-v* release workflow passes. Install the
          // locally-packed manifest/types so pre-release CI remains hermetic.
          manifest.dependencies['@murasakijs/native'] = `file:${process.env.NATIVE_TARBALL}`
          manifest.pnpm = {
            ...(manifest.pnpm ?? {}),
            overrides: {
              ...(manifest.pnpm?.overrides ?? {}),
              '@murasakijs/native': `file:${process.env.NATIVE_TARBALL}`,
              '@murasakijs/ui': `file:${process.env.UI_TARBALL}`,
            },
          }
          fs.writeFileSync(manifestPath, `${JSON.stringify(manifest, null, 2)}\n`)
          NODE

          pnpm --dir "$APP_DIR" install --no-frozen-lockfile
          pnpm --dir "$APP_DIR" lint
          pnpm --dir "$APP_DIR" typecheck
          pnpm --dir "$APP_DIR" build

  examples-quality:
    name: Examples / typecheck, test, build, PostgreSQL
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: murasaki
          POSTGRES_PASSWORD: murasaki-ci
          POSTGRES_DB: orglia_ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U murasaki -d orglia_ci"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 12
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: 24
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Build framework dependencies
        run: |
          pnpm --filter @murasakijs/ui build
          pnpm --filter murasaki build

      - name: Verify Papelle
        run: |
          pnpm --dir examples/papelle typecheck
          pnpm --dir examples/papelle test
          pnpm --dir examples/papelle build

      - name: Verify Oscilla
        run: |
          pnpm --dir examples/oscilla typecheck
          pnpm --dir examples/oscilla test
          pnpm --dir examples/oscilla build

      - name: Verify Orglia
        env:
          TEST_DATABASE_URL: postgresql://murasaki:murasaki-ci@127.0.0.1:5432/orglia_ci
        run: |
          pnpm --dir examples/orglia typecheck
          pnpm --dir examples/orglia test
          pnpm --dir examples/orglia test:postgres
          pnpm --dir examples/orglia build

      - name: Build Orglia production container
        run: docker build --tag orglia-ci examples/orglia

      - name: Smoke-test Orglia production container
        env:
          ORGLIA_BOOTSTRAP_PASSWORD: orglia-ci-container-password
        run: |
          docker run --detach --name orglia-ci \
            --env ORGLIA_BOOTSTRAP_PASSWORD \
            --publish 127.0.0.1:4173:4173 \
            orglia-ci
          trap 'docker rm --force orglia-ci >/dev/null 2>&1 || true' EXIT

          for attempt in {1..30}; do
            if curl --fail --silent --show-error \
              http://127.0.0.1:4173/healthz \
              > "$RUNNER_TEMP/orglia-health.json"; then
              break
            fi
            if [ "$attempt" = 30 ]; then
              docker logs orglia-ci
              exit 1
            fi
            sleep 1
          done

          HEALTH_PATH="$RUNNER_TEMP/orglia-health.json" node <<'NODE'
          const health = require(process.env.HEALTH_PATH)
          if (health.ok !== true || health.database !== 'sqlite') {
            throw new Error(`Unexpected Orglia health payload: ${JSON.stringify(health)}`)
          }
          NODE

  linux-native-build:
    name: Linux x64 / native release build, bundle smoke
    runs-on: ubuntu-22.04
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4

      - name: Install Linux native dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev librsvg2-dev libssl-dev libxdo-dev squashfs-tools xvfb dbus-x11

      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
        with:
          targets: x86_64-unknown-linux-gnu

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          workspaces: crates/native

      - name: Install native package dependencies
        working-directory: crates/native
        run: pnpm install --ignore-workspace

      - name: Test native crate on Linux
        working-directory: crates/native
        run: cargo test --locked

      - name: Build Linux native binding
        working-directory: crates/native
        run: pnpm exec napi build --platform --release --target x86_64-unknown-linux-gnu

      - name: Build Linux production launcher
        working-directory: crates/native
        run: cargo build --locked --release --bin murasaki-launcher --target x86_64-unknown-linux-gnu

      - name: Stage the launcher binary under its published napi-triple name
        working-directory: crates/native
        run: cp target/x86_64-unknown-linux-gnu/release/murasaki-launcher murasaki-launcher.linux-x64-gnu

      - name: Add a branch-local scaffold to the workspace
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p examples/ci-linux-app
          cp -r packages/create-murasaki/templates/default/. examples/ci-linux-app/
          node <<'NODE'
          const fs = require('node:fs')
          const path = 'examples/ci-linux-app/package.json'
          const manifest = JSON.parse(fs.readFileSync(path, 'utf8'))
          manifest.name = 'ci-linux-app'
          manifest.dependencies.murasaki = 'workspace:*'
          manifest.dependencies['@murasakijs/ui'] = 'workspace:*'
          fs.writeFileSync(path, `${JSON.stringify(manifest, null, 2)}\n`)

          fs.writeFileSync('examples/ci-linux-app/murasaki.config.ts', `import { defineConfig } from 'murasaki'

          export default defineConfig({
            appId: 'app.murasaki.ci.linux',
            productName: 'Murasaki CI Linux',
            version: '0.0.0',
            icon: 'src/assets/icon.png',
            locales: ['en'],
            protocols: [{ scheme: 'murasaki-ci-linux', name: 'Murasaki CI Linux link' }],
            fileAssociations: [{
              extensions: ['murasakici'],
              name: 'Murasaki CI Linux document',
              role: 'editor',
            }],
          })
          `)
          NODE
          node scripts/create-window-profile-smoke.mjs examples/ci-profile-linux linux-ci
      - name: Install workspace dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build framework packages
        run: |
          pnpm --filter @murasakijs/ui build
          pnpm --filter murasaki build

      - name: Build scaffolded app
        working-directory: examples/ci-linux-app
        run: pnpm exec murasaki build

      - name: Bundle scaffolded app (AppDir + AppImage)
        working-directory: examples/ci-linux-app
        run: pnpm exec murasaki bundle --target linux-x64

      - name: Bundle browser-profile isolation probe
        working-directory: examples/ci-profile-linux
        run: pnpm exec murasaki bundle --target linux-x64

      - name: Verify AppDir layout and AppImage artifact
        shell: bash
        working-directory: examples/ci-linux-app
        run: |
          set -euo pipefail
          APP_DIR=$(find dist/bundle -maxdepth 1 -type d -name '*.AppDir' -print -quit)
          test -n "$APP_DIR"
          echo "AppDir: $APP_DIR"

          test -x "$APP_DIR/AppRun"
          test -f "$APP_DIR/app.murasaki.ci.linux.desktop"
          test -f "$APP_DIR/app.murasaki.ci.linux.png"
          test -L "$APP_DIR/.DirIcon"
          test -x "$APP_DIR/usr/bin/Murasaki-CI-Linux"
          test -f "$APP_DIR/usr/share/applications/app.murasaki.ci.linux.desktop"
          test -f "$APP_DIR/usr/lib/app.murasaki.ci.linux/resources/murasaki-meta.json"
          test -x "$APP_DIR/usr/lib/app.murasaki.ci.linux/resources/node"
          grep -q 'MimeType=x-scheme-handler/murasaki-ci-linux;application/x-murasakici;' "$APP_DIR/app.murasaki.ci.linux.desktop"
          test -f "$APP_DIR/usr/share/icons/hicolor/256x256/apps/app.murasaki.ci.linux.png"

          APPIMAGE=$(find dist/bundle -maxdepth 1 -type f -name '*.AppImage' -print -quit)
          test -n "$APPIMAGE"
          test -x "$APPIMAGE"
          echo "AppImage: $APPIMAGE"

      - name: Verify persistent per-window browser-profile authority isolation
        shell: bash
        run: |
          set -euo pipefail
          APPIMAGE=$(find examples/ci-profile-linux/dist/bundle -maxdepth 1 -type f -name '*.AppImage' -print -quit)
          test -n "$APPIMAGE"
          for attempt in 1 2; do
            bash .github/scripts/linux-smoke-test.sh \
              "$APPIMAGE" \
              "$RUNNER_TEMP/murasaki-linux-profile-smoke-$attempt" \
              60 \
              "MURASAKI_WINDOW_PROFILE_ISOLATION_OK=service-worker-denied" \
              "true"
          done

      - name: Package the .deb installer
        working-directory: examples/ci-linux-app
        run: pnpm exec murasaki installer --target linux-x64 --no-build

      - name: Verify the .deb artifact
        shell: bash
        working-directory: examples/ci-linux-app
        run: |
          set -euo pipefail
          DEB=$(find dist -maxdepth 1 -type f -name '*.deb' -print -quit)
          test -n "$DEB"
          echo ".deb: $DEB"
          ar t "$DEB"

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: failure()
        with:
          name: linux-x64-bundle-failure
          path: |
            examples/ci-linux-app/dist/bundle/**
            examples/ci-linux-app/dist/*.deb
            examples/ci-profile-linux/dist/bundle/**
            ${{ runner.temp }}/murasaki-linux-profile-smoke-*/**
          if-no-files-found: warn

  macos-framework-native:
    name: macOS arm64 / framework, native, bundle smoke
    runs-on: macos-14
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          workspaces: crates/native

      - name: Add a branch-local scaffold to the workspace
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p examples/ci-mac-app
          cp -r packages/create-murasaki/templates/default/. examples/ci-mac-app/
          node <<'NODE'
          const fs = require('node:fs')
          const path = 'examples/ci-mac-app/package.json'
          const manifest = JSON.parse(fs.readFileSync(path, 'utf8'))
          manifest.name = 'ci-mac-app'
          manifest.dependencies.murasaki = 'workspace:*'
          manifest.dependencies['@murasakijs/ui'] = 'workspace:*'
          fs.writeFileSync(path, `${JSON.stringify(manifest, null, 2)}\n`)

          fs.writeFileSync('examples/ci-mac-app/murasaki.config.ts', `import { defineConfig } from 'murasaki'

          export default defineConfig({
            appId: 'app.murasaki.ci.mac',
            productName: 'Murasaki CI macOS',
            version: '0.0.0',
            icon: 'src/assets/icon.png',
            locales: ['en'],
            window: {
              title: 'Murasaki CI',
              width: 800,
              height: 600,
              capabilities: ['window:list', 'window:open'],
              backendCapabilities: ['api:POST:/api/multiwindow-probe'],
            },
            windows: {
              probe: {
                route: '/probe',
                title: 'Murasaki multi-window probe',
                width: 640,
                height: 480,
                visible: false,
                createOnLaunch: false,
                capabilities: ['window:getLabel', 'window:list'],
                backendCapabilities: ['api:POST:/api/multiwindow-probe'],
              },
            },
          })
          `)

          fs.writeFileSync('examples/ci-mac-app/src/app/page.tsx', `'use client'

          import { useEffect } from 'react'
          import { windows } from 'murasaki/native'

          let started = false

          export default function PrimaryProbe() {
            useEffect(() => {
              if (started) return
              started = true
              void (async () => {
                const before = await windows.list()
                if (before.map((window) => window.label).join(',') !== 'main') {
                  throw new Error('dormant probe was created on launch')
                }

                const staged = await fetch('/api/multiwindow-probe', {
                  method: 'POST',
                  headers: { 'content-type': 'application/json' },
                  body: JSON.stringify({ stage: 'primary' }),
                })
                if (!staged.ok) throw new Error('primary probe stage was rejected')
                let after = await windows.list()
                for (let attempt = 0; attempt < 100; attempt++) {
                  if (after.some((window) => window.label === 'probe')) break
                  await new Promise((resolve) => setTimeout(resolve, 50))
                  after = await windows.list()
                }
                const probe = after.find((window) => window.label === 'probe')
                if (!probe || probe.visible) throw new Error('recreated probe is not hidden')
                await windows.open('probe')
              })().catch((error) => console.error('MURASAKI_MULTIWINDOW_PRIMARY_FAIL', error))
            }, [])

            return <main>Primary multi-window probe</main>
          }
          `)

          fs.mkdirSync('examples/ci-mac-app/src/app/probe', { recursive: true })
          fs.writeFileSync('examples/ci-mac-app/src/app/probe/page.tsx', `'use client'

          import { useEffect } from 'react'
          import { appWindow, windows } from 'murasaki/native'

          let started = false

          export default function MultiWindowProbe() {
            useEffect(() => {
              if (started) return
              started = true
              void (async () => {
                const label = await appWindow.getLabel()
                let after = await windows.list()
                for (let attempt = 0; attempt < 100; attempt++) {
                  if (after.find((window) => window.label === 'probe')?.visible) break
                  await new Promise((resolve) => setTimeout(resolve, 50))
                  after = await windows.list()
                }
                const labels = after.map((window) => window.label).sort()
                const probe = after.find((window) => window.label === 'probe')

                if (label !== 'probe') throw new Error('secondary renderer has the wrong label')
                if (labels.join(',') !== 'main,probe') throw new Error('window list is incomplete')
                if (!probe?.visible) throw new Error('probe did not open')

                let denied = false
                try {
                  await appWindow.isVisible()
                } catch {
                  denied = true
                }
                if (!denied) throw new Error('ungranted window:isVisible call was allowed')

                const response = await fetch('/api/multiwindow-probe', {
                  method: 'POST',
                  headers: { 'content-type': 'application/json' },
                  body: JSON.stringify({
                    stage: 'secondary',
                    label,
                    labels,
                    denied,
                    opened: true,
                  }),
                })
                if (!response.ok) throw new Error('probe API rejected the renderer result')
              })().catch((error) => {
                const message = error instanceof Error ? error.message : String(error)
                console.error('MURASAKI_MULTIWINDOW_CLIENT_FAIL', message)
                void fetch('/api/multiwindow-probe', {
                  method: 'POST',
                  headers: { 'content-type': 'application/json' },
                  body: JSON.stringify({ stage: 'error', message }),
                })
              })
            }, [])

            return <main>Multi-window probe</main>
          }
          `)

          fs.mkdirSync('examples/ci-mac-app/src/api/multiwindow-probe', { recursive: true })
          fs.writeFileSync('examples/ci-mac-app/src/api/multiwindow-probe/route.ts', `import type { RouteHandler } from 'murasaki'
          import { windows as mainWindows } from 'murasaki/main'

          const marker = 'MURASAKI_MULTIWINDOW_OK=probe:main,probe:hidden-denied-opened'
          let primaryReady = false

          export const POST: RouteHandler = async (request) => {
            const body = await request.json() as Record<string, unknown>
            if (body.stage === 'error') {
              console.error('MURASAKI_MULTIWINDOW_CLIENT_FAIL=' + String(body.message))
              return Response.json({ ok: false }, { status: 500 })
            }
            if (body.stage === 'primary') {
              const dormant = await mainWindows.list()
              if (dormant.map((window) => window.label).join(',') !== 'main') {
                return Response.json({ ok: false }, { status: 400 })
              }
              const first = await mainWindows.create('probe')
              await mainWindows.destroy('probe')
              const second = await mainWindows.create('probe')
              const valid = first.label === 'probe'
                && second.label === 'probe'
                && second.generation > first.generation
                && second.visible === false
              if (!valid) return Response.json({ ok: false }, { status: 400 })
              primaryReady = true
              return Response.json({ ok: true, generation: second.generation }, { status: 202 })
            }

            const valid = primaryReady
              && body.stage === 'secondary'
              && body.label === 'probe'
              && JSON.stringify(body.labels) === JSON.stringify(['main', 'probe'])
              && body.denied === true
              && body.opened === true
            if (!valid) return Response.json({ ok: false }, { status: 400 })

            console.log(marker)
            return Response.json({ ok: true, marker })
          }
          `)
          NODE
          node scripts/create-window-profile-smoke.mjs examples/ci-profile-mac macos-ci

      - name: Install workspace dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build and test UI dependency
        run: pnpm --filter @murasakijs/ui test

      - name: Test Murasaki framework
        run: pnpm --filter murasaki test

      - name: Test native crate
        working-directory: crates/native
        run: cargo test --locked

      - name: Verify generated native loader and declarations
        working-directory: crates/native
        run: |
          pnpm exec napi build --platform --release
          git diff --exit-code -- index.js index.d.ts

      - name: Build production native launcher
        working-directory: crates/native
        run: cargo build --locked --release --bin murasaki-launcher

      - name: Build scaffolded app
        working-directory: examples/ci-mac-app
        run: pnpm exec murasaki build

      - name: Bundle scaffolded app
        working-directory: examples/ci-mac-app
        run: pnpm exec murasaki bundle --target darwin-arm64

      - name: Bundle browser-profile isolation probe
        working-directory: examples/ci-profile-mac
        run: pnpm exec murasaki bundle --target darwin-arm64

      - name: Locate bundled application
        id: bundle
        shell: bash
        run: |
          set -euo pipefail
          APP_PATH=$(find examples/ci-mac-app/dist/bundle -maxdepth 2 -type d -name '*.app' -print -quit)
          test -n "$APP_PATH"
          printf 'appPath=%s\n' "$APP_PATH" >> "$GITHUB_OUTPUT"

      - name: Launch bundled app and verify backend
        shell: bash
        run: >-
          bash .github/scripts/mac-smoke-test.sh
          "${{ steps.bundle.outputs.appPath }}"
          "$RUNNER_TEMP/murasaki-mac-smoke"
          60
          "MURASAKI_MULTIWINDOW_OK=probe:main,probe:hidden-denied-opened"
          "true"

      - name: Verify per-window browser-profile authority isolation
        shell: bash
        run: |
          set -euo pipefail
          APP_PATH=$(find examples/ci-profile-mac/dist/bundle -maxdepth 2 -type d -name '*.app' -print -quit)
          test -n "$APP_PATH"
          bash .github/scripts/mac-smoke-test.sh \
            "$APP_PATH" \
            "$RUNNER_TEMP/murasaki-mac-profile-smoke" \
            60 \
            "MURASAKI_WINDOW_PROFILE_ISOLATION_OK=service-worker-denied" \
            "true"

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: failure()
        with:
          name: macos-arm64-bundle-failure
          path: |
            ${{ runner.temp }}/murasaki-mac-smoke/**
            ${{ runner.temp }}/murasaki-mac-profile-smoke/**
            examples/ci-mac-app/dist/bundle/**/*.app/**
            examples/ci-profile-mac/dist/bundle/**/*.app/**
          if-no-files-found: warn

  windows-bundle-smoke:
    name: Windows x64 / native and bundle smoke
    runs-on: windows-2022
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4

      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4 # stable
        with:
          targets: x86_64-pc-windows-msvc

      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2
        with:
          workspaces: crates/native

      - name: Add a branch-local scaffold to the workspace
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p examples/ci-win-app
          cp -r packages/create-murasaki/templates/default/. examples/ci-win-app/
          node <<'NODE'
          const fs = require('node:fs')
          const path = 'examples/ci-win-app/package.json'
          const manifest = JSON.parse(fs.readFileSync(path, 'utf8'))
          manifest.name = 'ci-win-app'
          manifest.dependencies.murasaki = 'workspace:*'
          manifest.dependencies['@murasakijs/ui'] = 'workspace:*'
          fs.writeFileSync(path, `${JSON.stringify(manifest, null, 2)}\n`)

          fs.writeFileSync('examples/ci-win-app/murasaki.config.ts', `import { defineConfig } from 'murasaki'

          export default defineConfig({
            appId: 'app.murasaki.ci.win',
            productName: 'Murasaki CI Windows',
            version: '0.0.0',
            icon: 'src/assets/icon.png',
            locales: ['en'],
            window: {
              title: 'Murasaki CI',
              width: 800,
              height: 600,
              capabilities: ['window:list', 'window:open'],
              backendCapabilities: ['api:POST:/api/multiwindow-probe'],
            },
            windows: {
              probe: {
                route: '/probe',
                title: 'Murasaki multi-window probe',
                width: 640,
                height: 480,
                visible: false,
                createOnLaunch: false,
                capabilities: ['window:getLabel', 'window:list'],
                backendCapabilities: ['api:POST:/api/multiwindow-probe'],
              },
            },
          })
          `)

          fs.writeFileSync('examples/ci-win-app/src/app/page.tsx', `'use client'

          import { useEffect } from 'react'
          import { windows } from 'murasaki/native'

          let started = false

          export default function PrimaryProbe() {
            useEffect(() => {
              if (started) return
              started = true
              void (async () => {
                const before = await windows.list()
                if (before.map((window) => window.label).join(',') !== 'main') {
                  throw new Error('dormant probe was created on launch')
                }

                const staged = await fetch('/api/multiwindow-probe', {
                  method: 'POST',
                  headers: { 'content-type': 'application/json' },
                  body: JSON.stringify({ stage: 'primary' }),
                })
                if (!staged.ok) throw new Error('primary probe stage was rejected')
                let after = await windows.list()
                for (let attempt = 0; attempt < 100; attempt++) {
                  if (after.some((window) => window.label === 'probe')) break
                  await new Promise((resolve) => setTimeout(resolve, 50))
                  after = await windows.list()
                }
                const probe = after.find((window) => window.label === 'probe')
                if (!probe || probe.visible) throw new Error('recreated probe is not hidden')
                await windows.open('probe')
              })().catch((error) => console.error('MURASAKI_MULTIWINDOW_PRIMARY_FAIL', error))
            }, [])

            return <main>Primary multi-window probe</main>
          }
          `)

          fs.mkdirSync('examples/ci-win-app/src/app/probe', { recursive: true })
          fs.writeFileSync('examples/ci-win-app/src/app/probe/page.tsx', `'use client'

          import { useEffect } from 'react'
          import { appWindow, windows } from 'murasaki/native'

          let started = false

          export default function MultiWindowProbe() {
            useEffect(() => {
              if (started) return
              started = true
              void (async () => {
                const label = await appWindow.getLabel()
                let after = await windows.list()
                for (let attempt = 0; attempt < 100; attempt++) {
                  if (after.find((window) => window.label === 'probe')?.visible) break
                  await new Promise((resolve) => setTimeout(resolve, 50))
                  after = await windows.list()
                }
                const labels = after.map((window) => window.label).sort()
                const probe = after.find((window) => window.label === 'probe')

                if (label !== 'probe') throw new Error('secondary renderer has the wrong label')
                if (labels.join(',') !== 'main,probe') throw new Error('window list is incomplete')
                if (!probe?.visible) throw new Error('probe did not open')

                let denied = false
                try {
                  await appWindow.isVisible()
                } catch {
                  denied = true
                }
                if (!denied) throw new Error('ungranted window:isVisible call was allowed')

                const response = await fetch('/api/multiwindow-probe', {
                  method: 'POST',
                  headers: { 'content-type': 'application/json' },
                  body: JSON.stringify({
                    stage: 'secondary',
                    label,
                    labels,
                    denied,
                    opened: true,
                  }),
                })
                if (!response.ok) throw new Error('probe API rejected the renderer result')
              })().catch((error) => {
                const message = error instanceof Error ? error.message : String(error)
                console.error('MURASAKI_MULTIWINDOW_CLIENT_FAIL', message)
                void fetch('/api/multiwindow-probe', {
                  method: 'POST',
                  headers: { 'content-type': 'application/json' },
                  body: JSON.stringify({ stage: 'error', message }),
                })
              })
            }, [])

            return <main>Multi-window probe</main>
          }
          `)

          fs.mkdirSync('examples/ci-win-app/src/api/multiwindow-probe', { recursive: true })
          fs.writeFileSync('examples/ci-win-app/src/api/multiwindow-probe/route.ts', `import type { RouteHandler } from 'murasaki'
          import { windows as mainWindows } from 'murasaki/main'

          const marker = 'MURASAKI_MULTIWINDOW_OK=probe:main,probe:hidden-denied-opened'
          let primaryReady = false

          export const POST: RouteHandler = async (request) => {
            const body = await request.json() as Record<string, unknown>
            if (body.stage === 'error') {
              console.error('MURASAKI_MULTIWINDOW_CLIENT_FAIL=' + String(body.message))
              return Response.json({ ok: false }, { status: 500 })
            }
            if (body.stage === 'primary') {
              const dormant = await mainWindows.list()
              if (dormant.map((window) => window.label).join(',') !== 'main') {
                return Response.json({ ok: false }, { status: 400 })
              }
              const first = await mainWindows.create('probe')
              await mainWindows.destroy('probe')
              const second = await mainWindows.create('probe')
              const valid = first.label === 'probe'
                && second.label === 'probe'
                && second.generation > first.generation
                && second.visible === false
              if (!valid) return Response.json({ ok: false }, { status: 400 })
              primaryReady = true
              return Response.json({ ok: true, generation: second.generation }, { status: 202 })
            }

            const valid = primaryReady
              && body.stage === 'secondary'
              && body.label === 'probe'
              && JSON.stringify(body.labels) === JSON.stringify(['main', 'probe'])
              && body.denied === true
              && body.opened === true
            if (!valid) return Response.json({ ok: false }, { status: 400 })

            console.log(marker)
            return Response.json({ ok: true, marker })
          }
          `)
          NODE
          node scripts/create-window-profile-smoke.mjs examples/ci-profile-win windows-ci

      - name: Install workspace dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build framework packages
        run: |
          pnpm --filter @murasakijs/ui build
          pnpm --filter murasaki build

      - name: Test native crate
        working-directory: crates/native
        run: cargo test --locked

      - name: Build native Node binding
        working-directory: crates/native
        run: pnpm exec napi build --platform --release

      - name: Build production native launcher
        working-directory: crates/native
        run: cargo build --locked --release --bin murasaki-launcher

      - name: Build scaffolded app
        working-directory: examples/ci-win-app
        run: pnpm exec murasaki build

      - name: Bundle scaffolded app
        working-directory: examples/ci-win-app
        run: pnpm exec murasaki bundle --target win32-x64

      - name: Bundle browser-profile isolation probe
        working-directory: examples/ci-profile-win
        run: pnpm exec murasaki bundle --target win32-x64

      - name: Locate bundled executable
        id: bundle
        shell: pwsh
        run: |
          $exePath = Join-Path (Resolve-Path 'examples/ci-win-app/dist/bundle') 'Murasaki CI Windows/Murasaki CI Windows.exe'
          if (-not (Test-Path -LiteralPath $exePath -PathType Leaf)) {
            Write-Error "Bundled application executable was not produced at the expected path: $exePath"
            exit 1
          }
          "exePath=$exePath" | Out-File -FilePath $env:GITHUB_OUTPUT -Append -Encoding utf8

      - name: Launch bundled app and verify backend
        shell: pwsh
        run: >-
          .github/scripts/win-smoke-test.ps1
          -ExePath "${{ steps.bundle.outputs.exePath }}"
          -TimeoutSeconds 60
          -ExpectedMarker "MURASAKI_MULTIWINDOW_OK=probe:main,probe:hidden-denied-opened"
          -ExpectBackendCrash

      - name: Verify per-window browser-profile authority isolation
        shell: pwsh
        run: |
          $exePath = Join-Path (Resolve-Path 'examples/ci-profile-win/dist/bundle') 'Murasaki Profile Smoke/Murasaki Profile Smoke.exe'
          if (-not (Test-Path -LiteralPath $exePath -PathType Leaf)) {
            Write-Error "Profile-isolation application executable was not produced: $exePath"
            exit 1
          }
          # The preceding smoke intentionally terminates a live WebView2 tree.
          # On hosted Windows runners its out-of-process COM server can take a
          # moment to unregister; starting a second unrelated app immediately
          # can otherwise fail transiently with CO_E_SERVER_EXEC_FAILURE even
          # though both packaged apps are valid in isolation.
          Start-Sleep -Seconds 5
          .github/scripts/win-smoke-test.ps1 `
            -ExePath $exePath `
            -TimeoutSeconds 60 `
            -ExpectedMarker 'MURASAKI_WINDOW_PROFILE_ISOLATION_OK=service-worker-denied' `
            -ExpectBackendCrash

      - name: Generate ephemeral code-signing PFX
        shell: pwsh
        timeout-minutes: 2
        run: |
          # Generate the fixture entirely through .NET. This avoids the
          # interactive Windows certificate provider used by
          # New-SelfSignedCertificate and still exercises the release PFX path.
          $pfxPath = Join-Path $env:RUNNER_TEMP 'murasaki-ci-signing.pfx'
          $password = 'murasaki-ci-only'
          $rsa = [System.Security.Cryptography.RSA]::Create(2048)
          try {
            $subject = [System.Security.Cryptography.X509Certificates.X500DistinguishedName]::new(
              'CN=Murasaki CI Authenticode'
            )
            $request = [System.Security.Cryptography.X509Certificates.CertificateRequest]::new(
              $subject,
              $rsa,
              [System.Security.Cryptography.HashAlgorithmName]::SHA256,
              [System.Security.Cryptography.RSASignaturePadding]::Pkcs1
            )
            $request.CertificateExtensions.Add(
              [System.Security.Cryptography.X509Certificates.X509KeyUsageExtension]::new(
                [System.Security.Cryptography.X509Certificates.X509KeyUsageFlags]::DigitalSignature,
                $true
              )
            )
            $oids = [System.Security.Cryptography.OidCollection]::new()
            [void]$oids.Add([System.Security.Cryptography.Oid]::new('1.3.6.1.5.5.7.3.3'))
            $request.CertificateExtensions.Add(
              [System.Security.Cryptography.X509Certificates.X509EnhancedKeyUsageExtension]::new(
                $oids,
                $false
              )
            )
            $certificate = $request.CreateSelfSigned(
              [System.DateTimeOffset]::UtcNow.AddMinutes(-5),
              [System.DateTimeOffset]::UtcNow.AddDays(2)
            )
            try {
              [System.IO.File]::WriteAllBytes(
                $pfxPath,
                $certificate.Export(
                  [System.Security.Cryptography.X509Certificates.X509ContentType]::Pfx,
                  $password
                )
              )
            } finally {
              $certificate.Dispose()
            }
          } finally {
            $rsa.Dispose()
          }
          "MURASAKI_WINDOWS_CERTIFICATE_FILE=$pfxPath" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          "MURASAKI_WINDOWS_CERTIFICATE_PASSWORD=$password" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          # Keep PR CI independent of a public timestamp service. Unit tests
          # cover /tr + /td ordering; release users timestamp by default.
          'MURASAKI_WINDOWS_TIMESTAMP_URL=false' | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
          # GitHub-hosted Windows blocks when a self-signed root is added to
          # the trust store. This opt-in is hard-restricted to CI + PFX and
          # verifies the Authenticode hash and exact signer instead of trust.
          'MURASAKI_WINDOWS_CI_ALLOW_UNTRUSTED_CERTIFICATE=1' | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8

      - name: Install Windows installer toolchains
        shell: pwsh
        run: |
          choco install nsis -y
          "C:\Program Files (x86)\NSIS" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8
          dotnet tool install --global wix --version 4.0.6
          "$env:USERPROFILE\.dotnet\tools" | Out-File -FilePath $env:GITHUB_PATH -Append -Encoding utf8

      - name: Install WiX UI extension
        shell: pwsh
        run: wix extension add -g WixToolset.UI.wixext/4.0.6

      - name: Build and Authenticode-sign portable, NSIS, and MSI artifacts
        working-directory: examples/ci-win-app
        run: pnpm exec murasaki installer --target win32-x64 --no-build --sign

      - name: Verify signed artifacts and signed app startup
        shell: pwsh
        run: |
          $expectedCertificate = [System.Security.Cryptography.X509Certificates.X509Certificate2]::new(
            $env:MURASAKI_WINDOWS_CERTIFICATE_FILE,
            $env:MURASAKI_WINDOWS_CERTIFICATE_PASSWORD,
            [System.Security.Cryptography.X509Certificates.X509KeyStorageFlags]::EphemeralKeySet
          )
          function Assert-CiAuthenticodeIntegrity {
            param([Parameter(Mandatory = $true)][string]$Path)
            $signature = Get-AuthenticodeSignature -LiteralPath $Path
            $status = [string]$signature.Status
            if ($null -eq $signature.SignerCertificate) {
              throw "No embedded Authenticode signer on $Path"
            }
            if (@('Valid', 'NotTrusted', 'UnknownError') -notcontains $status) {
              throw "Authenticode integrity check failed on $Path`: $status $($signature.StatusMessage)"
            }
            if ($signature.SignerCertificate.Thumbprint -ne $expectedCertificate.Thumbprint) {
              throw "Unexpected Authenticode signer on $Path"
            }
          }

          $app = (Resolve-Path 'examples/ci-win-app/dist/bundle/Murasaki CI Windows/Murasaki CI Windows.exe').Path
          $setup = (Resolve-Path 'examples/ci-win-app/dist/Murasaki CI Windows-0.0.0-setup-x64.exe').Path
          $msi = (Resolve-Path 'examples/ci-win-app/dist/Murasaki CI Windows-0.0.0.msi').Path
          @($app, $setup, $msi) | ForEach-Object { Assert-CiAuthenticodeIntegrity -Path $_ }

          $zip = (Resolve-Path 'examples/ci-win-app/dist/bundle/Murasaki CI Windows-win32-x64.zip').Path
          $expanded = Join-Path $env:RUNNER_TEMP 'murasaki-signed-portable'
          Expand-Archive -LiteralPath $zip -DestinationPath $expanded -Force
          $portableExe = Join-Path $expanded 'Murasaki CI Windows/Murasaki CI Windows.exe'
          Assert-CiAuthenticodeIntegrity -Path $portableExe
          $expectedCertificate.Dispose()

          .github/scripts/win-smoke-test.ps1 `
            -ExePath $app `
            -TimeoutSeconds 60 `
            -ExpectedMarker 'MURASAKI_MULTIWINDOW_OK=probe:main,probe:hidden-denied-opened' `
            -ExpectBackendCrash

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: failure()
        with:
          name: windows-x64-bundle-failure
          path: examples/ci-win-app/dist/bundle/**
          if-no-files-found: warn
