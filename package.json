{
  "name": "murasaki-monorepo",
  "private": true,
  "version": "0.0.0",
  "description": "Monorepo root for murasaki — Next.js-DX desktop framework, Rust-native, React 19, Vite.",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r --filter='./packages/*' build",
    "dev": "pnpm --filter murasaki dev",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "pnpm": {
    "overrides": {
      "@hono/node-server": "2.0.12",
      "@modelcontextprotocol/sdk": "1.30.0",
      "fast-uri": "3.1.4",
      "sharp": "0.35.3",
      "postcss": ">=8.5.10"
    }
  }
}
