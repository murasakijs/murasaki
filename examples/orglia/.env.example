# Required by Docker Compose. Use long, unique values from a secret manager.
ORGLIA_BOOTSTRAP_PASSWORD=replace-me
ORGLIA_POSTGRES_PASSWORD=replace-me-too
# Optional when TLS terminates at a reverse proxy, for example:
# ORGLIA_PUBLIC_ORIGIN=https://orglia.example.com
